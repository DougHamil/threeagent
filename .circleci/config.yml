version: 2.1

orbs:
  welcome: circleci/welcome-orb@0.3.1

jobs:
  build:
    docker:
      - image: circleci/clojure:openjdk-11-lein-2.9.1
    steps:
      - checkout
      - run: echo "hello world"

workflows:
  Welcome:
    jobs:
      - build
